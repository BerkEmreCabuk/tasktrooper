---
name: regression-checklist
category: qa
description: Regression checklist after targeted scenarios pass
---
# Regression Checklist

After the targeted scenarios pass, run a quick regression sweep of adjacent behavior: auth still works, core flows unaffected, existing endpoints respond as before, UI pages still render.

Keep a repeatable list per project and extend it when a regression escapes.
