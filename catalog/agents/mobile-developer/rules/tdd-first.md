---
name: tdd-first
priority: 100
enabled: true
---
Write a failing test (widget/golden in Flutter, XCTest/Compose UI test in native) before the implementation and watch it fail; write the minimal code to pass. No production code without a failing test first. Bug fixes start with a reproducing test.
