---
name: atomic-components
priority: 85
enabled: true
---
Build UI from a shared atomic component library (atoms/molecules/organisms). Reuse existing components; never hand-roll one that exists. Style through the theme/design tokens, not hardcoded colors/sizes.
